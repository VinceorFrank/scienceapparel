// client/src/utils/config.js

// Must match server/utils/config.js
export const HOMEPAGE_SLOTS = [
  'hero',
  'infoA',
  'infoB',
  'featuredTop',
  'featuredBottom',
  'categoryClothing',
  // add more as needed
];

// You can also export the visibility enum if desired:
export const VISIBILITIES = ['visible', 'hidden', 'archived']; 